spring:
  application:
    name: BusTicketApp
  datasource:
    url: jdbc:postgresql://localhost:5432/busticket
    username: postgres
    password: root
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
stripe:
  secret-key: sk_test_...
springdoc:
  swagger-ui:
    url: /openapi.yaml
jwt:
  secret: your-secret-key-base64-encoded
  audience: bus-ticket-app
  issuer: bus-ticket-app